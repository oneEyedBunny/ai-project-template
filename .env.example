# Copy this file to .env and fill in real values. NEVER commit the real .env.
# .env is gitignored; this .example file is the committed template that documents
# which variables exist (without their secret values).

# Example — replace with this project's actual variables once the stack is chosen:
# API_BASE_URL=
# DATABASE_URL=
# AUTH_SECRET=
